[project]
name = "rkvc-bench"
version = "0.2.0"
description = "RK3588 rate-distortion benchmark plots for rkvc"
license = "AGPL-3.0-or-later"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
]
